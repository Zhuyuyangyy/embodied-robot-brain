# 专利技术交底书：基于物理硬闸门的具身智能冲突检测与验证系统

**一种基于物理反馈驱动δ阈值硬闸门的具身智能冲突检测与配伍验证方法**

---

## 1. 发明名称

**中文：** 一种基于物理硬闸门的具身智能冲突检测与中医配伍验证方法

**英文：** PhysicsGate: A Physical Feedback-Driven Hard Gate for Embodied AI Conflict Detection and TCM Compatibility Validation

---

## 2. 所属技术领域

本发明属于人工智能、具身智能（Embodied AI）与传统医学知识工程交叉领域。具体涉及一种通过物理反馈机制对大语言模型（LLM）生成的推理指令进行硬约束验证的系统和方法，尤其适用于中医诊疗、智能制药、医疗机器人等对安全性有严格要求的垂直场景。

---

## 3. 背景技术及现有技术的缺陷和不足

### 3.1 现有技术现状

LLM驱动的智能系统在科研助手（GPT-4 Academic）、多Agent编排（AutoGen、CrewAI）、具身机器人任务规划等领域已广泛应用。现有系统普遍采用"概率置信度"机制——以LLM生成的token概率分布作为决策依据，通过温度参数（Temperature）或Top-K/Top-P采样控制输出的随机性。

### 3.2 核心缺陷

| 缺陷类型 | 具体问题 | 危险性 |
|---------|---------|--------|
| **缺陷一：幻觉问题** | LLM对罕见药材配伍（如十八反）的认知来源于语料库统计，而非物理真实性校验 | 致命 |
| **缺陷二：软约束失效** | 现有系统以置信度阈值过滤，本质仍是概率过滤而非物理约束 | 高 |
| **缺陷三：无先验知识整合** | 缺乏将结构化知识（药典、禁忌、毒理学）编码为可执行约束的机制 | 高 |
| **缺陷四：状态转移失控** | Agent执行路径不受物理规律约束，LLM生成的指令序列可直接到达硬件执行层 | 极高 |
| **缺陷五：冲突检测碎片化** | 现有冲突检测仅针对单点问题（方法不一致/结果差异），缺乏统一的三层分类体系 | 中 |

### 3.3 关键问题

**核心问题：** 在具身智能系统中，LLM作为"大脑"生成的指令（如"开具甘草+甘遂配方"）若不经过物理真实性验证直接到达执行层，可能造成不可逆的物理后果（药物毒性）。

**现有解决方案的根本缺陷：** 依赖LLM的"自我认知"来避免错误（"模型已知则不生成"），而非建立独立于模型的物理验证层。

---

## 4. 核心技术创新点与数学推导

### 【创新点一】PhysicsGate — 物理反馈驱动的δ阈值硬闸门

#### 4.1.1 问题建模

设LLM在时刻t生成了包含n个药材实体的指令序列 A_t = {a_1, a_2, ..., a_n}，其中每个a_i为药材名称。设物理约束系统为约束库K（如TCMConstraintLibrary），包含m条物理/药理禁忌规则。

PhysicsGate将状态转移问题建模为：

**定义（PhysicsGate映射函数）：**

G(A_t, K, δ) = 
- HARD_REJECT, if E(A_t, K) = 1.0 (fatal)
- SOFT_REJECT, if E(A_t, K) >= δ and E < 1.0  
- PASS, if E(A_t, K) < δ

其中E(A_t, K)为误差值（冲突严重度），定义为：

E(A_t, K) = max{ severity_score(a_i, a_j, K) | for all i < j }

severity_score定义如下：

| 严重度 | 分值 | 典型案例 |
|--------|------|---------|
| fatal（致命） | E = 1.0 | 甘草+甘遂（十八反）|
| high（高度） | E = 0.7 | 硫黄+朴硝（十九畏）|
| medium（中等） | E = 0.4 | 黄连+附子（寒热冲突）|
| low（低） | E = 0.1 | 相恶配伍 |

#### 4.1.2 δ阈值动态衰减机制

**技术特征：** 为防止物理闸门在边缘案例上产生死锁，引入动态δ阈值衰减机制：

δ(t) = 0.05, when retry(t) < 3
δ(t) = 0.10, when retry(t) >= 3

**技术效果：** 前3次重试采用严格阈值（δ=0.05），确保安全；3次以上自动放宽至0.10，允许边缘案例通过验证，避免系统死锁。**致命冲突（E=1.0）不受δ衰减影响，任何重试次数下均HARD_REJECT。**

#### 4.1.3 物理闸门工作流程

```
LLM生成指令 A_t
       |
       v
PhysicsGate.check(A_t, K)
       |
       v
计算 E(A_t, K) = max severity_score
       |
       +-- E = 1.0 (fatal)  --> HARD_REJECT --> 终止执行
       |
       +-- E >= δ 且 E < 1.0 --> SOFT_REJECT --> 返回ConflictFeedback --> 重试
       |
       +-- E < δ --> PASS --> 允许状态转移
```

### 【创新点二】TCMConstraintLibrary — 中医配伍禁忌硬约束库

#### 4.2.1 三层禁忌体系

**第一层：十八反（致命禁忌，共21对）**

基于《神农本草经》《本草纲目》等经典药典，收录药性相杀的药对组合：

| 禁忌群 | 代表药对 | 毒性机制 |
|--------|---------|---------|
| 甘草反甘遂系 | 甘草与甘遂/大戟/海藻/芫花 | 甘草酸与萜类协同增毒 |
| 乌头反贝蒌系 | 川乌/草乌/附子与贝母/瓜蒌/半夏/白蔹 | 生物碱与甙类沉淀减效 |
| 人参反五灵脂 | 人参与五灵脂 | 皂苷与黏液质结合 |

**第二层：十九畏（高度禁忌，共9对）**

| 药对 | 合用后果 |
|------|---------|
| 硫黄+朴硝 | 泻下作用过强 |
| 水银+砒霜 | 砷汞协同毒性 |
| 人参+五灵脂 | 人参益气功散 |

**第三层：药性冲突（中等禁忌，共6对）**

| 冲突类型 | 药对 | 药性对立 |
|---------|------|---------|
| 寒热冲突 | 黄连(寒)与附子(热) | 四气对立 |
| 升降冲突 | 麻黄(升)与赭石(降) | 作用趋势对立 |

#### 4.2.2 约束查询算法

```python
def check_contraindication(h1: str, h2: str) -> Optional[PhysicsConstraint]:
    p = (h1, h2)
    # O(1)哈希查找，非遍历
    if p in self._contra:     # 十八反哈希集合
        return PhysicsConstraint(severity="fatal", E=1.0, ...)
    if p in self._awe:        # 十九畏哈希集合
        return PhysicsConstraint(severity="high", E=0.7, ...)
    if p in self._coldhot:    # 寒热冲突哈希集合
        return PhysicsConstraint(severity="medium", E=0.4, ...)
    return None
```

**查询复杂度：** O(1) 哈希查找，时间复杂度与禁忌对数量无关。

### 【创新点三】TransferSignal — 三值状态转移信号

**技术特征：** 定义三种互斥状态信号，替代传统二值（True/False）判定：

| 信号 | 语义 | 系统行为 |
|------|------|---------|
| PASS | 物理约束通过 | 允许状态转移至下一级 |
| SOFT_REJECT | 中等冲突，可重试 | 返回ConflictFeedback + 修复建议，允许重试 |
| HARD_REJECT | 致命冲突，不可绕过 | 终止执行链路，触发安全告警 |

**状态转移方程：**

S_{t+1} = 
- S_t (HARD_REJECT时终止)
- RETRY(S_t) (SOFT_REJECT时重试)
- A_{t+1} (PASS时状态转移)

### 【创新点四】三层冲突分类体系与四维可信度评分

#### 4.4.1 三层冲突分类

| 类型 | 名称 | 判断标准 | 检测来源 |
|------|------|---------|---------|
| Type-I | 方法不一致冲突 | 同一数据集上方法A与方法B的性能差异 > 2.0% | ConflictDetector.detect_type_I() |
| Type-II | 结果差异冲突 | 同一方法在数据集D1与D2上的性能差异 > 5.0% | ConflictDetector.detect_type_II() |
| Type-III | 理论矛盾冲突 | 两种理论假设在逻辑上互斥，无法同时为真 | ConflictDetector.detect_type_III() + PhysicsGate |

#### 4.4.2 四维可信度评分

S_paper = w_c * S_citation + w_r * S_recency + w_cs * S_consistency + w_kg * S_kg

| 维度 | 权重 | 含义 |
|------|------|------|
| Citation | w_c=0.25 | 引用影响力，归一化到[0,1] |
| Recency | w_r=0.20 | 时效性，半衰期5年指数衰减 |
| Consistency | w_cs=0.30 | 方法一致性，跨数据集效果稳定性 |
| KG | w_kg=0.25 | 知识图谱一致性，与TCMConstraintLibrary的符合度 |

---

## 5. 具体实施方式

### 5.1 系统架构

```
LLM Brain（推理引擎）
输入：用户指令（如"开具含甘草和甘遂的处方"）
         |
         v
PhysicsGate（物理硬闸门）
TCMConstraintLibrary -> 十八反/十九畏/寒热冲突哈希库
δ阈值 = 0.05（严格）/ 0.10（重试放宽）
         |
    +----+----+
    |         |
    v         v
HARD_REJECT  SOFT_REJECT   PASS
(终止执行)   (重试+反馈)  (状态转移)
```

### 5.2 核心算法伪代码

```python
class PhysicsGate:
    def __init__(self, constraint_library=None, delta=0.05):
        self.cl = constraint_library or TCMConstraintLibrary()
        self.delta = delta
        self._retry = defaultdict(int)  # 按task_id计数

    def check(self, entities: List[str], task_id="default") -> PhysicsGateResult:
        # Step 1: 计算最大误差值（最严重冲突）
        E = self._formula_error(entities)

        # Step 2: 获取当前δ（动态衰减）
        retry = self._retry[task_id]
        d = 0.10 if retry >= 3 else self.delta

        # Step 3: 查约束库获取冲突详情
        constraint = self._find_constraint(entities)

        # Step 4: 信号判定（致命冲突直接HARD_REJECT）
        if E >= 1.0:
            signal = TransferSignal.HARD_REJECT
        elif E >= d:
            signal = TransferSignal.SOFT_REJECT
            self._retry[task_id] += 1
        else:
            signal = TransferSignal.PASS

        # Step 5: 生成反馈
        feedback = ConflictFeedback(...) if signal != TransferSignal.PASS else None
        return PhysicsGateResult(signal, E, d, feedback, retry)
```

### 5.3 典型应用场景

**场景一：中医智能诊疗系统**

- 用户输入："患者脾虚，给开含人参和五灵脂的方子"
- PhysicsGate检测：人参与五灵脂 属于十九畏，severity=high，E=0.7
- δ=0.05，E>=δ -> SOFT_REJECT
- 反馈：建议去除五灵脂或换用其他活血药
- LLM重试后生成合规方剂

**场景二：中药新药发现**

- LLM生成候选化合物组合
- PhysicsGate预验证组合的药理安全性
- 只有通过物理闸门的组合才进入湿实验验证

---

## 6. 权利要求书

### 独立权利要求

**权利要求1：** 一种基于物理硬闸门的具身智能冲突检测方法，其特征在于，包括以下步骤：

（a）接收大语言模型生成的包含多个实体对象的指令序列 A_t；

（b）建立物理约束知识库K，包含至少一层禁忌规则，其中所述禁忌规则以哈希集合形式存储，支持O(1)时间复杂度的冲突查询；

（c）计算所述指令序列A_t中任意两个实体之间的冲突严重度得分E；

（d）将所述得分E与动态阈值δ进行比较，当E达到预设致命阈值1.0时，输出第一信号（HARD_REJECT）；当E介于所述致命阈值与所述动态阈值之间时，输出第二信号（SOFT_REJECT）；当E低于所述动态阈值时，输出第三信号（PASS）；

（e）根据所述信号控制状态转移：所述第一信号终止执行链路，所述第二信号触发重试机制并返回修复建议，所述第三信号允许执行链路继续。

**权利要求2：** 如权利要求1所述的方法，其特征在于，所述动态阈值δ根据重试次数动态调整：当重试次数小于3次时，δ取第一预设值0.05；当重试次数大于或等于3次时，δ取第二预设值0.10；其中致命冲突的E值为1.0时不受所述动态阈值调整影响，任何重试次数下均输出所述第一信号。

### 从属权利要求

**权利要求3：** 如权利要求1所述的方法，其特征在于，所述物理约束知识库K为中医配伍禁忌库，包括：十八反禁忌层（至少18对相反药对）、十九畏禁忌层（至少9对相畏药对）、药性冲突层（寒热冲突、升降冲突中的一种或多种）。

**权利要求4：** 如权利要求3所述的方法，其特征在于，所述十八反禁忌层中的药对包括：甘草与甘遂/大戟/海藻/芫花中的任意一味；川乌/草乌/附子与贝母/瓜蒌/半夏/白蔹中的任意一味；人参与五灵脂。

**权利要求5：** 如权利要求1所述的方法，其特征在于，所述冲突严重度得分E通过以下方式确定：E(A_t, K) = max{ severity_score(a_i, a_j, K) | for all i < j }，其中severity_score的取值为：致命冲突对应1.0，高度冲突对应0.7，中度冲突对应0.4，低度冲突对应0.1。

**权利要求6：** 一种系统，包括：物理硬闸门模块（PhysicsGate），配置为执行权利要求1所述方法的步骤(a)-(e)；约束知识库模块（TCMConstraintLibrary），存储所述物理约束知识库K；状态转移控制模块，配置为根据所述第一信号、第二信号或第三信号执行相应控制动作。

**权利要求7：** 如权利要求6所述的系统，其特征在于，还包括三层冲突分类模块，配置为：Type-I冲突检测（同一数据集上不同方法的性能差异超过第一预设阈值）、Type-II冲突检测（同一方法在不同数据集上的性能差异超过第二预设阈值）、Type-III冲突检测（两个理论假设在逻辑上互斥）。

**权利要求8：** 如权利要求6所述的系统，其特征在于，所述约束知识库模块还包括四维可信度评分单元，配置为根据引用影响力、时效性、方法一致性和知识图谱一致性四个维度计算文献可信度得分。

**权利要求9：** 一种计算机可读存储介质，其特征在于，存储计算机程序，所述计算机程序被处理器执行时实现如权利要求1-5任一项所述的方法。

**权利要求10：** 一种大语言模型推理加速器，其特征在于，包括权利要求6所述的系统的硬件实现，其中所述哈希集合查找操作通过布隆过滤器或硬件查找表实现。

---

## 7. 技术效果总结

| 效果指标 | 现有技术 | 本发明 |
|---------|---------|--------|
| 冲突检测精度（Precision） | 无法保证 | **100%**（对已知禁忌对） |
| 误判率 | 高（依赖LLM自我认知） | **0%**（硬编码知识库） |
| 响应时间 | LLM推理延迟（秒级） | O(1)哈希查找（微秒级） |
| 状态转移安全性 | 无保证 | 三值信号硬性约束 |
| 知识可扩展性 | 依赖微调/提示工程 | 独立约束库，可热更新 |

---

*文档版本：v1.0*
*生成时间：2026-05-04*
*关联代码：backend/agents/conflict_validator.py（PhysicsGate + TCMConstraintLibrary + TransferSignal）*
